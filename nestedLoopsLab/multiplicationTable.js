function multiplicationTable(){

    for (let i = 1; i <= 10; i++) {
        for (let k = 1; k <= 10; k++) {
            
            let diff = i * k;
        console.log(`${i} * ${k} = ${diff}`);
    }
        }
       
    }


multiplicationTable()